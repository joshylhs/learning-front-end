[project]
name = "flight-booking-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "joshylhs", email = "joshlow35@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=2.0.0",
]

[project.scripts]
flight-booking-server = "flight_booking_server:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
